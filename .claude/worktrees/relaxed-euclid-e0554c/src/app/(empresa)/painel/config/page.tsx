import EmBreve from "@/components/EmBreve";
import { Settings } from "lucide-react";
export default function ConfigPage() {
  return <EmBreve titulo="Configurações" descricao="Configurações da empresa, horários de funcionamento, integrações e personalização." icone={Settings} />;
}
