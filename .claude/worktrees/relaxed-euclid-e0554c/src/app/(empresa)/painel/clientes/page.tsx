import EmBreve from "@/components/EmBreve";
import { Users } from "lucide-react";
export default function ClientesPage() {
  return <EmBreve titulo="Clientes (CRM)" descricao="Histórico de pedidos por cliente, programa de fidelidade e comunicação." icone={Users} />;
}
