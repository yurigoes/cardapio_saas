import EmBreve from "@/components/EmBreve";
import { Users } from "lucide-react";
export default function AdminUsuariosPage() {
  return <EmBreve titulo="Usuários Master" descricao="Gestão de usuários internos da plataforma (acesso administrativo)." icone={Users} />;
}
