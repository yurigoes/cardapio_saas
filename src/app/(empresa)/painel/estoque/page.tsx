import EmBreve from "@/components/EmBreve";
import { Package } from "lucide-react";
export default function EstoquePage() {
  return <EmBreve titulo="Estoque" descricao="Controle de insumos, baixa automática por pedido e alertas de reposição." icone={Package} />;
}
