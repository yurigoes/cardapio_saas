import EmBreve from "@/components/EmBreve";
import { ChefHat } from "lucide-react";
export default function CozinhaPage() {
  return <EmBreve titulo="Cozinha (KDS)" descricao="Display da cozinha com fila de produção, temporizadores e status dos pedidos." icone={ChefHat} />;
}
