import EmBreve from "@/components/EmBreve";
import { CreditCard } from "lucide-react";
export default function GatewaysPage() {
  return <EmBreve titulo="Pagamentos" descricao="Configure gateways de pagamento: cartão, PIX, dinheiro e outros meios." icone={CreditCard} />;
}
